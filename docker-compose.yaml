services:
  streamlit:
    build: .
    environment:
      - BASE_API_URL=http://app:8000
    ports:
      - "8501:8501"
    volumes:
      - .:/app
      - /app/node_modules
    command: ["streamlit", "run", "app.py"]
